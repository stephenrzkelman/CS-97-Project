import React from 'react';
import Exercise from '../../database/Exercise.js';

class SearchBar extends React.Component {
	render(){
		return(

		);
	}
}
// use Exercise.find(id) to find by tag/id
export default SearchBar;
